import { LanguageCodePipe } from './language-code.pipe';

describe('LanguageCodePipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageCodePipe();
    expect(pipe).toBeTruthy();
  });
});
