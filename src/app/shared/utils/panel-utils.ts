/**
 * Used to fill the shared panel component.
 * @title the string used as panel title.
 * @intro the string used as panel summary of the content.
 * @text the string used as main content of the panel.
 */
export interface Panel {
  title: string;
  intro: string;
  text: string;
}
