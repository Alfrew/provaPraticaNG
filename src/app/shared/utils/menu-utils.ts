/**
 * Used for menu list of items.
 * @icon the file icon name.
 * @label the string that is visible in the UI.
 * @matIcon the mat icon name.
 * @value the property passed as value.
 */
export interface MenuItem {
  icon?: string;
  label: string;
  matIcon?: string;
  value: string;
}
